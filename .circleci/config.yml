version: 2.1

orbs:
  aws-cli: circleci/aws-cli@1.2.1
  slack: circleci/slack@3.4.2

workflows:
  lint:
    jobs:
      - lint:
          context: aws
      - build-and-deploy-charms-on-lxd:
          context: aws
          requires:
            - lint

jobs:
  lint:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run:
          command: pip install tox
      - run:
          name: Run Lint
          command: |
            make lint

  build-and-deploy-charms-on-lxd:
    machine:
      image: ubuntu-1604:202004-01
    steps:
      - checkout

      - run:
          name: "Install snapd"
          command: |
            sudo apt-get update
            sudo apt-get install --only-upgrade -y snapd
      - run:
          name: "Install charmcraft"
          command: |
            sudo snap install charmcraft --beta
      - run:
          name: "clone charms"
          command: |
            git clone git@github.com:omnivector-solutions/slurm-charms.git
      - run:
          name: "prepare repo"
          command: |
            rm ./slurm-charms/charm-slurmdbd/requirements.txt
            echo  "ops" > ./slurm-charms/charm-slurmdbd/requirements.txt
            echo  "git+https://github.com/omnivector-solutions/interface-mysql.git@master" > ./slurm-charms/charm-slurmdbd/requirements.txt
            cat ./slurm-charms/charm-slurmdbd/requirements.txt
      - run:
          name: "Install LXD"
          command: |
            sudo apt-get purge lxd lxd-client lxcfs
            sudo snap install lxd
            sudo lxd init --auto
            sleep 5
            sudo /snap/bin/lxd.lxc network set lxdbr0 ipv6.address none
      - run:
          name: "Install Juju"
          command: |
            sudo apt-get purge juju
            sudo snap install juju --classic
      - run:
          name: "Install juju-wait"
          command: |
            sudo snap install juju-wait --classic
      - run:
          name: "Install jq"
          command: |
            sudo snap install jq
      - run:
          name: "Bootstrap Juju"
          command: |
            /snap/bin/juju bootstrap localhost
            /snap/bin/juju model-config logging-config="<root>=DEBUG;<unit>=DEBUG"

